name: CI/CD React App

on:
  push:
    branches:
      - main

permissions:
  contents: write   # cần để push vào nhánh gh-pages

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout source
        uses: actions/checkout@v3

      - name: 🏗 Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18
          cache: "npm"

      - name: 📦 Install dependencies
        run: npm ci

      - name: 🛠 Build project
        run: npm run build

      - name: 📤 Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: react-build
          path: ./build   # với CRA. Nếu dùng Vite thì đổi thành ./dist

  deploy:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: 📥 Download build artifact
        uses: actions/download-artifact@v4
        with:
          name: react-build
          path: ./build   # với CRA, Vite thì ./dist

      - name: 🚀 Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./build   # với CRA, Vite thì ./dist
